/**
 * Tells the React Native CLI where our font files live.
 *
 * After adding or removing a font here, run:
 *   npx react-native-asset
 *
 * That copies the fonts into the Android and iOS projects. Fonts are baked into
 * the app itself, so a rebuild is needed afterwards — Metro reload is not enough.
 */
module.exports = {
  assets: ['./src/assets/fonts'],
};
