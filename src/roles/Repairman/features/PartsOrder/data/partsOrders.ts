/**
 * Demo parts-order data. When the API arrives, replace this file with the
 * fetch and keep the same shape — the screen reads these fields and nothing
 * else.
 */

export const PARTS_ORDER_STATS = {
  totalOrders: 0,
  pendingOrders: 0,
  deliveredOrders: 0,
  totalAmount: 0,
};

export const PARTS_ORDERS = [];
